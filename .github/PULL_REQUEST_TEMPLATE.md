## What this changes

<!-- Short description of the change -->

## Why

<!-- What problem this solves, or link the issue: Fixes #123 -->

## How to test

<!-- Steps to verify this works -->

## Checklist

- [ ] Ran `node --check` on changed files (or the app starts without errors)
- [ ] Ran `npm run db:migrate` if `database/schema.sql` changed
- [ ] Ran `npm run deploy-commands` if any command definition changed
- [ ] User-facing bot messages use the i18n system (`src/i18n`), not hardcoded strings
- [ ] No em dashes, no emoji in the PR title
